<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUI3Example.RevealFocusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:local="using:WinUI3Example"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Style="{StaticResource ControlExamplesStackPanelStyle}">
        <local:ControlExample HeaderText="RevealFocus basic example">
            <essential:RevealFocusPanel x:Name="RevealFocusPanel" Height="500">
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="Button">
                            <Setter Property="Width" Value="100" />
                            <Setter Property="Height" Value="100" />
                            <Setter Property="VerticalAlignment" Value="Top" />
                            <Setter Property="CornerRadius" Value="4" />
                            <Setter Property="Margin" Value="10" />
                        </Style>
                    </StackPanel.Resources>
                    <Button essential:RevealFocusPanel.AttachToPanel="{x:Bind RevealFocusPanel}" Content="Button 1" />
                    <Button essential:RevealFocusPanel.AttachToPanel="{x:Bind RevealFocusPanel}" Content="Button 2" />
                    <Button essential:RevealFocusPanel.AttachToPanel="{x:Bind RevealFocusPanel}" Content="Button 3" />
                </StackPanel>
            </essential:RevealFocusPanel>
        </local:ControlExample>

        <local:ControlExample HeaderText="RevealFocus on ListView">
            <essential:RevealFocusPanel x:Name="ListViewRevealFocusPanel" Height="400">
                <ListView
                    x:Name="BaseListView"
                    Width="350"
                    Height="400"
                    HorizontalAlignment="Left"
                    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                    BorderThickness="1"
                    ContainerContentChanging="BaseListView_ContainerContentChanging"
                    Tag="{x:Bind ListViewRevealFocusPanel}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:Contact">
                            <TextBlock
                                Margin="0,5,0,5"
                                x:Phase="1"
                                Text="{x:Bind Name}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </essential:RevealFocusPanel>
        </local:ControlExample>

        <local:ControlExample HeaderText="RevealFocus on GridView">
            <essential:RevealFocusPanel x:Name="GridViewRevealFocusPanel">
                <GridView
                    x:Name="BaseGridView"
                    Width="300"
                    ContainerContentChanging="BaseListView_ContainerContentChanging"
                    Tag="{x:Bind GridViewRevealFocusPanel}" />
            </essential:RevealFocusPanel>
        </local:ControlExample>

        <local:ControlExample HorizontalContentAlignment="Stretch" HeaderText="RevealFocus on NavigationView">
            <essential:RevealFocusPanel x:Name="NavigationViewRevealFocusPanel">
                <NavigationView>
                    <NavigationView.MenuItems>
                        <NavigationViewItem
                            essential:RevealFocusPanel.AttachToPanel="{x:Bind NavigationViewRevealFocusPanel}"
                            Content="Menu Item1"
                            Icon="Play"
                            Tag="SamplePage1" />
                        <NavigationViewItem
                            essential:RevealFocusPanel.AttachToPanel="{x:Bind NavigationViewRevealFocusPanel}"
                            Content="Menu Item2"
                            Icon="Save"
                            Tag="SamplePage2" />
                        <NavigationViewItem
                            essential:RevealFocusPanel.AttachToPanel="{x:Bind NavigationViewRevealFocusPanel}"
                            Content="Menu Item3"
                            Icon="Refresh"
                            Tag="SamplePage3" />
                        <NavigationViewItem
                            essential:RevealFocusPanel.AttachToPanel="{x:Bind NavigationViewRevealFocusPanel}"
                            Content="Menu Item4"
                            Icon="Download"
                            Tag="SamplePage4" />
                    </NavigationView.MenuItems>
                    <Frame />
                </NavigationView>
            </essential:RevealFocusPanel>
        </local:ControlExample>
    </StackPanel>
</Page>
