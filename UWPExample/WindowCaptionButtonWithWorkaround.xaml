<Page
    x:Class="UWPExample.WindowCaptionButtonWithWorkaround"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:UWPPackage"
    xmlns:local="using:UWPExample"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <essential:WindowCaptionButtonThemeWorkaround />
        <TextBlock
            Margin="0,32,46,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            FontSize="32"
            Text="Notice these Caption Buttons when switching themes ⬆️" />

        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="This is a default UWP Window with CaptionButtonThemeWorkaround" />

        <local:ThemeToggleButton VerticalAlignment="Bottom" Root="{x:Bind}" />
    </Grid>
</Page>
